import 'package:flutter/material.dart';

class BarPackaging extends StatelessWidget {
  const BarPackaging({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
